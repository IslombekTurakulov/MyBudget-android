<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/pin_container_padding">

    <com.chaos.view.PinView
        android:id="@+id/pinView"
        style="@style/PinWidget.PinView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusable="true"
        android:inputType="number"
        android:autofillHints="oneTimeCode"
        android:contentDescription="@string/code_hint"

        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"

        app:itemCount="4"
        app:itemWidth="@dimen/pin_item_size"
        app:itemHeight="@dimen/pin_item_size"
        app:itemSpacing="@dimen/pin_item_spacing"
        app:itemBackground="@drawable/bg_pin_item"

        app:lineColor="@color/overlay"
        app:lineWidth="@dimen/pin_line_width"
        app:lineSpacing="@dimen/pin_line_spacing"
        android:cursorVisible="true"
        app:hideLineWhenFilled="false"
        app:cursorColor="@color/black" />
</androidx.constraintlayout.widget.ConstraintLayout>
