<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="FullscreenImageDialog" parent="@style/ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/FullscreenDialogAnimation</item>
    </style>

    <style name="FullscreenDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/modal_in</item>
        <item name="android:windowExitAnimation">@anim/modal_out</item>
    </style>
</resources>